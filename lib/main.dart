import 'package:flutter/material.dart';

void main() {
  runApp(const FantasyFootball());
}

class FantasyFootball extends StatelessWidget {
  const FantasyFootball({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
      ),
    );
  }
}